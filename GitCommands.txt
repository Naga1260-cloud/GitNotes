
git config --global init.defaultBranch main
git init
git add
git commit
git push
git pull
git diff
git status

git remote

Create github account


configuring git

1. ssh-keygen -t rsa -b 4096 -C "yaram.nlakshmi@gmail.com"
2. cat ./ssh/id_rsa.pub
3. Copy content
4. Update Git setting ssh key with the copied public key
5. ssh -T git@github.com

Crate a repo in Github.com

For existing local repository

1. git remote add origin git@github.com:Naga1260-cloud/GitNotes.git
2. git branch -M main
3. git push -u origin main

These are for your github account

If you are working as a guest contribuer we can user username and password
username of the ropo owner
pat personal access token for that user

All public repositories can be cloned and work locally

git clone https://github.com/Naga1260-cloud/GitNotes.git

git clone 
